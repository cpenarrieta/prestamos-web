import React, { PropTypes } from 'react';

function BankDashboard(props) {
  return (
    <div>bank dashboard</div>
  );
}

BankDashboard.propTypes = {
  onSubmit: PropTypes.func,
};

export default BankDashboard;
